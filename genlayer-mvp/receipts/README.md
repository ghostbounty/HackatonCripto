# Receipts

El baseline activo separa:

- decision del contrato: `APPROVED`, `REJECTED`, `NEEDS_REVISION`, `UNDETERMINED`
- estado visible del caso: `accepted`, `finalized`, `needs_revision`, `undetermined`
- estado tecnico del protocolo o receipt

La UI no debe colapsar `accepted` y `finalized` en una sola etiqueta.
