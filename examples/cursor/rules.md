# Ejemplo para Cursor

## Reglas sugeridas

- Leer `AGENTS.md` al iniciar cualquier tarea.
- Usar `skills/` para identificar la etapa activa del flujo.
- No mover `docs/product/` ni `changes/` existentes.
- Mantener cada PR alineado a un solo `changes/<change-name>/`.
